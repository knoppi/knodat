#!/usr/bin/env python

from MultiMap import MultiMap
import numpy as np
import matplotlib.pyplot as plt
import matplotlib.cm as cm

filename = 

columns

    summe = summe + i
print " = ", summe

fig = plt.figure( dpi=150 )
diag = fig.add_subplot( 111 )
#X, Y, Z, extent = viso.retrieve3dPlotData( "x", "y", "lambda", interpolation=None, bin_distance=2, averaging=15  )
#density = diag.imshow( Z, extent = extent, cmap=cm.RdBu )
#fig.colorbar( density )

#X, Y, U, V = current.retrieveQuiverPlotData( "x", "y", "j0x", "j0y", bin_distance=5, averaging=5 )
#X, Y, U, extent = particles.retrieve3dPlotData( "x", "y", "d0" )
#X, Y, Z1, extent = current.retrieve3dPlotData( "x", "y", "j0", bin_distance = 5 )
#X, Y, Z2, extent = viso.retrieve3dPlotData( "x", "y", "V", bin_distance = 5 )
#V = zeros( U.shape )
#diag.quiver( X, Y, U, V, units="inches", angles="xy", scale=0.4, width=0.01 )
#combined = diag.imshow( Z1*Z2, extent = extent, cmap=cm.RdBu )
#combined = diag.imshow( Z2, extent = extent, cmap=cm.RdBu )
#fig.colorbar( combined )
plt.show()
